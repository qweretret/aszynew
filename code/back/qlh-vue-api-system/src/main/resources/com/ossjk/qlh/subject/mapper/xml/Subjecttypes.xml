<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ossjk.qlh.subject.mapper.SubjecttypesMapper">

	<!-- 通用查询映射结果 -->
	<resultMap id="BaseResultMap" type="com.ossjk.qlh.subject.entity.Subjecttypes">
		<result column="crtm" property="crtm" />
		<result column="mdtm" property="mdtm" />
		<result column="crer" property="crer" />
		<result column="mder" property="mder" />
		<result column="id" property="id" />
		<result column="name" property="name" />
		<result column="pid" property="pid" />
		<result column="level" property="level" />
		<result column="idex" property="idex" />
	</resultMap>

	<!-- 通用查询结果列 -->
	<sql id="Base_Column_List">
		crtm,
		mdtm,
		crer,
		mder,
		id,
		name,
		pid,
		level,
		idex
	</sql>

<!--	<select id="selectByUid" resultType="com.ossjk.qlh.subject.entity.Subjecttypes">-->
<!--		select * from department d where-->
<!--		d.id in (select rd.did from role_department rd where-->
<!--		rd.rid in ( select ur.rid from user_role ur where ur.uid = #{uid})) ORDER BY d.level,d.sort-->
<!--	</select>-->

</mapper>
